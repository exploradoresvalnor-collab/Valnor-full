// Test setup file
import { beforeAll, afterEach, afterAll } from 'vitest';

// Setup global test environment
beforeAll(() => {
  // Any global setup
});

afterEach(() => {
  // Cleanup after each test
});

afterAll(() => {
  // Any global cleanup
});